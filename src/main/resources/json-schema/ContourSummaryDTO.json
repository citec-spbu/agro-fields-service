{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "ContourSummaryDTO",
  "type": "object",
  "properties": {
    "contourId": {
      "type": "string",
      "description": "The contour ID",
      "readOnly": true
    },
    "color": {
      "type": "string",
      "pattern": "^([A-Fa-f0-9]{6})$",
      "description": "The color must be written in HEX format"
    },
    "squareArea": {
      "type": "string",
      "description": "The area of the contour"
    },
    "coordinates": {
      "type": "array",
      "items": {
        "$ref": "CoordinatesDTO.json"
      },
      "description": "Coordinates of the contour"
    }
  },
  "required": ["color", "coordinates"]
}
